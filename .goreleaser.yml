version: 2

project_name: spark

builds:
  - id: spark
    main: ./cmd/spark
    binary: spark
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X spark/internal/version.Version={{ .Version }}
      - -X spark/internal/version.GitCommit={{ .Commit }}
      - -X spark/internal/version.BuildDate={{ .Date }}

archives:
  - id: spark-binaries
    ids:
      - spark
    formats:
      - binary
    name_template: "spark-{{ .Os }}-{{ .Arch }}"

release:
  draft: false
  prerelease: auto

checksum:
  name_template: "checksums.txt"
